



{% extends 'base.html.twig' %}

{% block title %}Inscription - Mandingue-Chic{% endblock %}

{% block body %}
<section class="py-16 flex justify-center">
    <div class="w-full max-w-lg bg-white p-8 rounded-lg shadow-lg">
        <h1 class="text-4xl font-bold text-center text-gray-800">👤 Créez votre compte</h1>
        <p class="text-center text-gray-600 mb-6">Rejoignez-nous et profitez des meilleures offres !</p>

        <!-- 🔥 Affichage des messages flash -->
        {% for message in app.flashes('error') %}
            <div class="bg-red-500 text-white p-4 rounded-lg mb-4 text-center">
                ⚠️ {{ message }}
            </div>
        {% endfor %}

        {# {% for message in app.flashes('success') %}
            <div class="bg-green-500 text-green p-4 rounded-lg mb-4 text-center">
                ✅ {{ message }}
            </div>
        {% endfor %} #}

        {{ form_start(registrationForm, { attr: { class: "space-y-6" } }) }}

        <!-- 🔥 Affichage des erreurs globales -->
        <div class="text-red-500 text-center text-sm font-semibold">
            {{ form_errors(registrationForm) }}
        </div>

        <div class="space-y-4">
            <!-- 🔥 Champ Nom -->
            <div>
                <label class="block text-gray-700 font-semibold">Nom</label>
                {{ form_widget(registrationForm.name, { attr: { class: "w-full px-4 py-3 border rounded-lg text-gray-900 focus:ring focus:ring-yellow-500", placeholder: "Entrez votre nom" } }) }}
                <p class="text-red-500 text-sm mt-1">{{ form_errors(registrationForm.name) }}</p>
            </div>

            <!-- 🔥 Champ Email -->
            <div>
                <label class="block text-gray-700 font-semibold">Email</label>
                {{ form_widget(registrationForm.email, { attr: { class: "w-full px-4 py-3 border rounded-lg text-gray-900 focus:ring focus:ring-yellow-500", placeholder: "Entrez votre email" } }) }}
                <p class="text-red-500 text-sm mt-1">{{ form_errors(registrationForm.email) }}</p>
            </div>

           <!-- 🔥 Champ Téléphone avec message d'erreur -->
<div>
    <label class="block text-gray-700 font-semibold">Téléphone</label>
    {{ form_widget(registrationForm.phone, { attr: { class: "w-full px-4 py-3 border rounded-lg text-gray-900 focus:ring focus:ring-yellow-500", placeholder: "Votre numéro de téléphone" } }) }}
    <p class="text-red-500 text-sm mt-1">
        {{ form_errors(registrationForm.phone) }}
    </p>
</div>

            <!-- 🔥 Champ Adresse -->
            <div>
                <label class="block text-gray-700 font-semibold">Adresse</label>
                {{ form_widget(registrationForm.address, { attr: { class: "w-full px-4 py-3 border rounded-lg text-gray-900 focus:ring focus:ring-yellow-500", placeholder: "Votre adresse complète" } }) }}
                <p class="text-red-500 text-sm mt-1">{{ form_errors(registrationForm.address) }}</p>
            </div>

           <!-- 🔥 Champ Mot de passe avec message d'erreur -->
<div>
    <label class="block text-gray-700 font-semibold">Mot de passe</label>
    {{ form_widget(registrationForm.plainPassword, { attr: { class: "w-full px-4 py-3 border rounded-lg text-gray-900 focus:ring focus:ring-yellow-500", placeholder: "Mot de passe sécurisé" } }) }}
    <p class="text-red-500 text-sm mt-1">
        {{ form_errors(registrationForm.plainPassword) }}
    </p>
</div>

            <!-- 🔥 Case à cocher Conditions Générales -->
            <div class="flex items-center space-x-2">
                {{ form_widget(registrationForm.agreeTerms, { attr: { class: "rounded border-gray-300 focus:ring focus:ring-yellow-500" } }) }}
                <label class="text-gray-700 text-sm">
                    J'accepte les <a href="#" class="text-yellow-500 font-semibold hover:underline">conditions générales</a>.
                </label>
            </div>
            <p class="text-red-500 text-sm mt-1">{{ form_errors(registrationForm.agreeTerms) }}</p>
        </div>

        <!-- 🔥 Bouton d'inscription avec animation -->
        <button class="w-full bg-yellow-500 text-black font-bold py-3 px-6 rounded-lg mt-4 hover:bg-yellow-600 transition transform hover:scale-105">
            S'inscrire ✨
        </button>

        {{ form_end(registrationForm) }}

        <p class="text-center text-gray-600 mt-4">
            Déjà un compte ? <a href="{{ path('app_login') }}" class="text-yellow-500 font-bold hover:underline">Connectez-vous</a>
        </p>
    </div>
</section>
{% endblock %}












