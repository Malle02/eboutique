{% extends 'base.html.twig' %}

{% block title %}Erreur {{ status_code }} - Mandingue-Chic{% endblock %}

{% block body %}
<section class="py-20 text-center">
    <div class="max-w-4xl mx-auto bg-white bg-opacity-90 p-10 rounded-lg shadow-2xl">
        <h1 class="text-6xl font-bold text-red-500 mb-6">{{ status_code }}</h1>
        <h2 class="text-3xl font-bold text-gray-800 mb-4">{{ status_text }}</h2>
        <p class="text-gray-600 text-lg mb-8">
            Une erreur est survenue. Nous nous excusons pour la gêne occasionnée.
        </p>
        
        <div class="flex justify-center space-x-4">
            <a href="{{ path('home') }}" 
               class="bg-yellow-500 text-black font-semibold py-3 px-6 rounded-lg hover:bg-yellow-600 transition duration-300 shadow-lg">
                Retour à l'accueil
            </a>
            
        </div>
    </div>
</section>
{% endblock %}